{
	"required": {
        "allowedValues":{
            "part" : {
                "type": "string",
                "info": "The part parameter specifies the videoCategory resource properties that the API response will include. Set the parameter value to snippet. ",
                "allowedValues": {
                    "snippet": {
                        "type": "boolean"
                    }
                }
            }
        }
	},
	"filters" : {
		"note": "specify exactly one of the following parameters",
		"allowedValues": {
			"id": {
				"type": "string",
				"info": "The id parameter specifies a comma-separated list of video category IDs for the resources that you are retrieving."
			},
			"regionCode": {
				"type": "boolean",
				"info": "The regionCode parameter instructs the API to return the list of video categories available in the specified country. The parameter value is an ISO 3166-1 alpha-2 country code."
			}
		}
	},
	"optionalParameters": {
		"allowedValues": {
			"h1": {
				"type": "string",
				"info": "The hl parameter instructs the API to retrieve localized resource metadata for a specific application language that the YouTube website supports. The parameter value must be a language code included in the list returned by the i18nLanguages.list method."
			}
		}
	}
}
